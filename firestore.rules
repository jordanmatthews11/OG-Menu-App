rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for any signed-in user across all collections.
    // App-level authorization is enforced via the authorizedUsers collection in the app.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
